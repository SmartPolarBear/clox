var ints:list[integer]=list{1,5,4,3,2};

for(var i=0;i<5;i++){
    print(ints[i]);
}

/*
ints.append(10);
*/

fun len2(lst:list[integer]){
    return len(lst);
}

print(len2(ints));
print(len(ints));

//for(var data:integer in ints){
//    print data;
//}
